import express from "express";
import mongoose from "mongoose";


// Sign In users
export const signIn = (req,res) => {
    res.send("welocome to sigin page")
}

// register  In users
export const register = (req,res) => {
    res.send("welocome to register page")
}